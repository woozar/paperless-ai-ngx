# MCP Server Configuration
MCP_PORT=3001
MCP_HOST=localhost

# Optional: Server authentication token for /login endpoint
# If not set, /login endpoint accepts requests without API token validation
# API_TOKEN=your-secret-server-token

# Optional: JWT secret for token signing
# If not set, a random secret will be generated on server startup
# WARNING: Generating a new secret on each restart invalidates all existing tokens
# JWT_SECRET=your-secure-random-secret

# Optional: Logging level (none, error, info, debug)
# Default: info
# - none: No logs (useful for tests)
# - error: Only errors
# - info: Errors + important events (recommended for production)
# - debug: All logs including detailed request information (recommended for development)
# LOG_LEVEL=info

# Note: Paperless-ngx credentials are NOT stored in env variables
# They must be provided via the /login endpoint
